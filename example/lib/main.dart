// Nothing here matters. The point of this project is its pubspec.yaml:
// the pinned versions produce real dependency conflicts against pub.dev,
// so the sidebar and the conflict panel have something to show.
void main() {}
